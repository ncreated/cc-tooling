# requirements.txt Template

Generate a `requirements.txt` with one package per line. Include only packages gathered during the interview.

If the user opted for tests, include `pytest`.

Example:
```
pyyaml
requests
```
