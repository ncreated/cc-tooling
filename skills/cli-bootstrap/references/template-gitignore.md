# .gitignore Template

Generate a `.gitignore` with this content:

```
venv/
__pycache__/
.pytest_cache/
*.pyc
```

If the tool produces output artifacts in a specific directory (e.g. `workspace/`, `output/`), add that directory too.
